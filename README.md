Build Cmake project using BUILD croosplatform :

    cmake -S . -B build && cmake --build build 

Build Cmake project using Make just for Linux :
    
    cmake -S . -B build && make   

./ba_example ../RS.png ../RSMov30cm.png
or
./build/ba_example data/RS.png data/RSMov30cm.png

PRESS Q to finish
